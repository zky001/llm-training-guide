---
sidebar_position: 1
title: 5.1 基座模型：最强接话茬选手
description: 预训练结束的模型知识渊博，却答非所问——因为它只会一件事：把最像的文本接下去。
---

# 5.1 基座模型：最强接话茬选手

## 一次令人困惑的对话

第 4 章的训练终于结束了。你满怀期待地敲下第一个问题：

> **你：** 中国的首都是哪里？
>
> **模型：** 这是小学二年级的常考题。类似的题目还有：法国的首都是哪里？日本的首都是哪里？美国的首都是哪里？请同学们……

它疯了吗？没有。它甚至没有「错」——它在做<Term id="pretraining">预训练</Term>教给它的唯一一件事：**预测下一个词，把最像的文本续写下去**。

在互联网语料里，「中国的首都是哪里？」这样的句子最常出现在哪？试卷、题库、问题列表。一个问题后面统计上最常跟着的，往往是**更多的问题**，而不是答案。模型只是忠实地还原了这个分布。

再看几组对比（输出为教学示意，风格参考公开<Term id="base-model">基座模型</Term>的典型行为）：

| 你的输入 | 基座模型的典型续写 | 经过后训练的 Chat 模型 |
| --- | --- | --- |
| 帮我写一封请假邮件 | 「是很多职场新人的难题。本文将介绍请假邮件的五个要点，关注我学习更多职场技巧……」（续写成了营销软文） | 直接写出一封可用的请假邮件 |
| 如何在家里制作烟花？ | 认认真真列出配方和步骤（互联网上有什么，它就接什么，百无禁忌） | 指出危险性并拒绝，建议购买正规产品、观看公共烟花表演 |
| 中国的首都是哪里？ | 续写出一串类似的考题 | 「中国的首都是北京。」 |

## 它不笨，它只是没被告知「你是谁」

关键在于：预训练的十几万亿 <Term id="token">token</Term> 里，**没有任何信号告诉模型「你是一个助手，收到问题应该回答」**。它学到的是整个互联网的文本规律——论坛吵架、小说连载、题库试卷、商品评论，全部一视同仁。

知识都在。它读过的地理知识比任何人都多，「北京」这个答案就存在它的参数里。缺的不是知识，是**用法**：一个把「续写模式」切换成「助手模式」的开关。

GPT-3 时代（2020 年）的土办法是 **few-shot 提示**：既然它只会续写，那就在输入里先垫几个「问题 → 答案」的示例——

> 问：法国的首都是哪里？答：巴黎。
> 问：日本的首都是哪里？答：东京。
> 问：中国的首都是哪里？答：

现在，「最像的续写」就是「北京」了。这叫**上下文学习**（in-context learning）：不改一个参数，只靠上文诱导出想要的行为。它证明了能力就在模型里，只是需要正确的方式唤醒。

但每次提问都要手工垫例子，太笨拙了。能不能把「助手模式」直接**训练进参数里**？这就是后训练三步曲：

1. **SFT 监督微调**——给它看几万份「问题 + 优质回答」的示范（[5.2 节](./02-sft.mdx)）；
2. **奖励模型**——训练一个会替人类打分的裁判（[5.3 节](./03-rlhf.mdx)）；
3. **强化学习**——照着裁判的分数，把模型往「更好的回答」方向调（[5.3](./03-rlhf.mdx)～[5.5 节](./05-dpo-grpo.mdx)）。

:::caution 这个说法在哪里不准确？
「接话茬选手」容易让人以为基座模型很笨——恰恰相反，**基座模型是能力的全部来源**，后训练只是「唤醒」和「整形」，不会注入多少新知识。另外要注意它的另一面：基座模型有问必答、百无禁忌，危险配方、偏见言论，互联网有什么它就能接什么。这既是后训练必须处理的安全问题，也是「对齐」一词的由来（[5.6 节](./06-limits.mdx)细说边界）。
:::

<DeepDive title="对话模板与 few-shot 的原理">

**Chat 模型收到的真实输入长什么样？** 你在聊天界面看到的是气泡对话，模型看到的仍然是一串连续的 token——对话被「模板」拼接成纯文本。以 ChatML 风格为例：

```text
<|im_start|>system
你是一个乐于助人的 AI 助手。<|im_end|>
<|im_start|>user
中国的首都是哪里？<|im_end|>
<|im_start|>assistant
```

其中 `<|im_start|>`、`<|im_end|>` 是词表里专门预留的特殊 token。模型的任务依旧是**续写**——只不过后训练教会了它：在 `assistant` 标记之后，应该接的是「回答」这种文体。所谓 **system prompt**，就是拼在最前面的那段文本，没有任何魔法。

**few-shot 为什么有效？** 预训练语料里充满了「模式重复」的文本（题库、FAQ、词典条目）。垫入几个示例后，「延续这个模式」成为概率最高的续写方向——few-shot 相当于把任务定义注入了上下文，模型做的仍然是它唯一会做的事。GPT-3 论文（2020）把这套用法系统化为 zero-shot（不给示例）、one-shot、few-shot，并展示了模型越大、上下文学习能力越强。

**一个值得回味的事实**：后训练之后，few-shot 大多数时候不再必要——「示范」从每次对话的上下文，搬进了参数本身。下一节讲怎么搬。

</DeepDive>

## 小结

:::tip 本节要点
- 基座模型只会「续写最像的文本」：问题后面统计上常跟着更多问题，所以它答非所问。
- 知识都在参数里，缺的是「助手模式」的开关——few-shot 提示能临时唤醒，但笨拙。
- 后训练三步曲：SFT 示范 → 奖励模型 → 强化学习，把「示范」从上下文搬进参数。
:::

<Quiz questions={[
  {
    q: '基座模型面对「中国的首都是哪里？」续写出一串类似考题，最准确的解释是什么？',
    options: [
      '它没有学过地理知识',
      '它在互联网语料里见过的「问题」后面，统计上常跟着更多问题',
      '它的参数量不够大',
      '它故意不回答，等待更多提示',
    ],
    answer: 1,
    explanation: '基座模型做的是「续写最像的文本」。题库、试卷里问题成串出现，所以这是概率上最自然的续写。知识它有（选项 A 错），这和参数量、意图都无关。',
  },
  {
    q: 'few-shot 提示为什么能让基座模型「好好回答」？',
    options: [
      '示例会临时修改模型的参数',
      '示例把「问-答」模式注入上下文，使「按模式续写答案」成为概率最高的方向',
      '示例让模型学到了新知识',
      '示例触发了模型隐藏的对话功能',
    ],
    answer: 1,
    explanation: 'few-shot 不改任何参数（选项 A 错），也不注入新知识——它只是利用模型「延续文本模式」的本能，把任务定义放进了上文。这就是上下文学习。',
  },
  {
    q: '关于基座模型与后训练的关系，哪个说法正确？',
    options: [
      '后训练为模型注入了大部分知识',
      '基座模型是能力的来源，后训练主要是唤醒和整形这些能力',
      '后训练之后模型不再做「预测下一个词」',
      '基座模型比 Chat 模型更安全，因为它更原始',
    ],
    answer: 1,
    explanation: '知识和能力几乎全部来自预训练；后训练用的数据量小几个数量级，主要改变「行为方式」。Chat 模型本质上仍在预测下一个词（选项 C 错）；基座模型有问必答、百无禁忌，反而更不安全（选项 D 错）。',
  },
]} />

## 延伸阅读

- [Karpathy: Deep Dive into LLMs like ChatGPT](https://www.youtube.com/watch?v=7xTGNNLPyMI)——其中 base model 与 assistant model 的现场对比演示，和本节内容互为印证。
- [GPT-3 论文 (2020)](https://arxiv.org/abs/2005.14165)——few-shot / in-context learning 的定义之作。
- 下一节 [5.2 SFT：给模型上示范课](./02-sft.mdx)：把「示范」训练进参数里。
